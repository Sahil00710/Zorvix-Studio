# Zorvix Frontend

TanStack Start frontend for the Zorvix Studio website.

## Commands

```bash
npm run dev
npm run typecheck
npm run build
```

## Environment

Create `frontend/.env` if needed:

```env
VITE_API_BASE_URL=http://localhost:5000
```
